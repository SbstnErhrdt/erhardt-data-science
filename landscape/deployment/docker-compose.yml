services:
  patent-landscape-worker:
    image: registry.erhardt.net/erhardt-patent-landscape-worker
    environment:
      # IMPORTANT: use Kong’s in-cluster address, not the public gateway
      SUPABASE_URL: http://kong:8000
      SUPABASE_KEY: ${SUPABASE_KEY}
    networks: [ proxy ]

networks:
  proxy:
    external: true